import { actionTypes } from '../constants/homeConstants';

export function requestNews() {
  return {
    type: actionTypes.HOME_REQUEST_NEWS,
    payload: 'Your imports are also incorrect, youll want to do'
  };
}
